# Decisiones Push D1-D6 (aprendidas)

> Sincronizado desde `docs/factory_push/factory_push_validaciones.md`. Push está fuera del v0 operativo.

- **D1** — Channel rellena `PurchaseContract` existente, no se crea contenedor por channel.
- **D2** — Eva define en frontend qué admite el contenedor (es la API ofrecida).
- **D3** — Modo A: damos doc, channel se adapta JSON propio (Push In Genérico, PDES-98).
- **D4** — Modo B: nosotros adaptamos su API (Dingus XML, SiteMinder SOAP/OTA).
- **D5** — Default NO extender contenedor por 1 solo proveedor. ≥3 proveedores piden lo mismo → backlog.
- **D6** — Hotel "rebelde" (entra por channel sin que Perla lo cree): frontend notificación pendiente.

## Decisiones D7+ (espacio para crecer)

_(vacío — se llena con cada `factory-close` Push)_
