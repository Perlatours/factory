-- Placeholder. Seed real se popula en Paso C (factory bootstrap).
-- Si necesitas re-seedear: docker compose down -v && docker compose up -d
SELECT 'seed placeholder - run db/seed-candidates.sh after init' AS status;
